\documentclass[
$if(chapter)$
  chapter,
$endif$
$if(print)$
  print,
$endif$
$if(lineno)$
  lineno,
$endif$
$if(index)$
  index,
$endif$
$if(papersize)$
  $papersize$,
$endif$
$if(page-layout)$
  $page-layout$,
$endif$
$if(flushleft)$
  flushleft,
$endif$
$if(geometry)$
  custommargin,
$endif$
$if(fontsize)$
  $fontsize$,
$endif$
$if(fontfamily)$
  $fontfamily$,
$endif$
]{PhDThesisPSnPDF}

\ifsetCustomMargin
  \usepackage[$for(geometry)$$geometry$$sep$,$endfor$]{geometry}
  \setFancyHdr
\fi

\title{$title$}

\author{$author$}

\begin{document}

\maketitle

\frontmatter

\tableofcontents

\listoffigures

\listoftables

\printnomenclature

\mainmatter

$body$

\printthesisindex

\end{document}
